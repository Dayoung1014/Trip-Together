<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.ssafy.enjoytrip.board.model.mapper.BoardMapper">

	<resultMap type="boardDto" id="article">
		<result column="id" property="id"/>
		<result column="user_id" property="userId"/>
		<result column="name" property="userName"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="hit" property="hit"/>
		<result column="write_time" property="writeTime"/>
	</resultMap>
	
	<resultMap type="boardDto" id="viewArticle" extends="article">
		<collection property="fileInfos" column="id" javaType="list" ofType="fileInfoDto" select="fileInfoList"/>
	</resultMap>
	
	<resultMap type="fileInfoDto" id="file">
		<result column="save_folder" property="saveFolder"/>
		<result column="original_file" property="originalFile"/>
		<result column="save_file" property="saveFile"/>
	</resultMap>

	<insert id="writeArticle" parameterType="boardDto">
		insert into notice (user_id, title, content, hit, write_time)
		values (#{userId}, #{title}, #{content}, 0, now())
	</insert>

	<sql id="search">
		<if test="word != null and word != ''">
			<if test="key == 'title'">
				and title like concat('%', #{word}, '%')
			</if>
			<if test="key != 'title'">
				and ${key} = #{word}
			</if>
		</if>
	</sql>

	<select id="listArticle" parameterType="map" resultMap="article">
		select b.id, b.user_id, b.title, b.content, b.hit, b.write_time, m.name
		from notice b, user m 
		where b.user_id = m.id
		<include refid="search"></include>
		order by b.id desc
		limit #{start}, #{listsize}
	</select>
	
	<select id="getTotalArticleCount" parameterType="map" resultType="int">
		select count(id)
		from notice
		<where>
			<include refid="search"></include>
		</where>
	</select>
	
	<select id="getArticle" parameterType="int" resultMap="article">
		select b.id, b.user_id, b.title, b.content, b.hit, b.write_time, m.name 
		from notice b, user m
		where b.user_id = m.id
		and b.id = #{id}
	</select>
	
	<select id="fileInfoList" resultMap="file">
		select save_folder, original_file, save_file
		from file_info
		where id = #{id}
	</select>
	
	<update id="updateHit" parameterType="int">
		update notice
		set hit = hit + 1
		where id = #{id}
	</update>
	
	<update id="modifyArticle" parameterType="boardDto">
		update notice
		set title = #{title}, content = #{content}
		where id = #{id}
	</update>
	
	<delete id="deleteFile" parameterType="int">
		delete from file_info
		where id = #{id}
	</delete>
	
	<delete id="deleteArticle" parameterType="int">
		delete from notice
		where id = #{id}
	</delete>

</mapper>